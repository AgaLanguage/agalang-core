on: push
name: Clippy check

env:
  RUSTFLAGS: "-Dwarnings"

jobs:
  clyppy_check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run clippy
        run: cargo clippy --all-targets --all-features